[user]
	name = Roman Blanco
	email = roman.blanco@actimo.com
[push]
	default = simple
[pull]
	rebase = true
[core]
	excludesfile = .gitignore
	filemode = false
	editor = nvim
[alias]
	f = "!git fetch --all --prune && git checkout master && git merge upstream/master --ff-only && git push"
[commit]
	template = /home/rblanco/.gitmessage
